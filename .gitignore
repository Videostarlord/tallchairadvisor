# Dependencies
node_modules/

# Build output
dist/

# Astro
.astro/

# Environment
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Credentials — never commit
credentials/

# Claude Code session scratch — worktrees, local settings. Never committed.
.claude/

# Playwright storageState for Amazon Associates (P3).
# A LIVE BEARER CREDENTIAL for a financial account — never commit it.
# Captured by hand, uploaded via `gh secret set AMAZON_STORAGE_STATE`, then deleted.
amazon-state.json
*-storage-state.json
