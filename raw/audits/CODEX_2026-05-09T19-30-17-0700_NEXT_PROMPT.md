# Next Prompt
Model: CODEX
Timestamp: 2026-05-09T19-30-17-0700

Use this in a new implementation session, not in read-only mode:

```text
You are working in the Tall Chair Advisor repo.

Goal:
Stabilize the TCA system before any further autonomous SEO/content work.

Work in implementation mode, but keep scope tight.

Do not add new content pages.
Do not redesign the whole system.
Do not touch raw historical archives except to read them.

Implement only Phase 1 stabilization:

1. Fix the current critical defect in `scripts/agents/execute-fixes.ts`.
   - Inspect the file carefully.
   - Remove any invalid smart quotes or Unicode that breaks executable code.
   - Preserve behavior unless the behavior is clearly broken.

2. Remove the author-page sitemap contradiction.
   - Inspect `src/pages/author/jackson-christopher/index.astro`
   - Inspect `astro.config.mjs`
   - Either:
     - keep the author page `noindex` and exclude it from sitemap-related surfaces, or
     - make it indexable and self-canonicalize it
   - Also inspect `public/llms.txt` and keep it consistent with the chosen decision.

3. Add a hard data-validity guard to `scripts/gsc-pull.ts`.
   - Do not overwrite `data/gsc/latest.json` if the pull is obviously incomplete.
   - Add clear logging explaining why the write was skipped.
   - Keep the change minimal and deterministic.

4. Normalize slash/no-slash page variants inside the GSC pipeline.
   - Start with the smallest safe improvement:
     - canonical URL normalization in analysis logic
     - no broad rewrite
   - Avoid changing historical raw archives.

5. Add a small validation note to the weekly workflow docs or strategy-facing system docs so future agents do not assume stale or partial GSC data is safe.

Rules:
- Read the repo first.
- Make only the smallest changes needed for Phase 1 stabilization.
- Use apply_patch for edits.
- Do not make speculative refactors.
- Run only read-only validation commands plus safe local checks.
- Summarize exactly what changed and what remains unfixed.
```
